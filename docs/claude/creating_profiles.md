## Creating Claude Profiles

Use the `-p` flag to specify a new profile name. If the profile doesn't exist, InBox will create it.

```bash
# Create a 'personal' profile for Claude
inbox claude -p personal
```