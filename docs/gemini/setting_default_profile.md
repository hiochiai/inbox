## Setting a Default Gemini Profile

If you frequently use a specific profile, you can set it as the default.

```bash
# Set the 'work' profile for Gemini as the default
inbox profile set-default gemini work
```