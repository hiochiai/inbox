## Using Antigravity with GOOGLE_CLOUD_PROJECT

If you need to specify a Google Cloud project, use the `GOOGLE_CLOUD_PROJECT` environment variable.

### Usage

```bash
GOOGLE_CLOUD_PROJECT=my-project-id inbox antigravity
```

This will set the project for the current session.
